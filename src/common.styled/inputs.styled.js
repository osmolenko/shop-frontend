import styled from 'styled-components'
import {blackText} from "./colors.styled";

export const searchInput = styled.input`
  font-size: 16px;
  line-height: 24px;
  color: ${blackText};
`

export const NewsInput = styled.input`
  font-family: Noto Sans;
  font-size: 16px;
  font-style: normal;
  font-weight: 400;
  line-height: 24px;
  letter-spacing: 0em;
  text-align: left;
  color: ${blackText};
`